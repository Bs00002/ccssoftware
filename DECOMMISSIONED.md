# DECOMMISSIONED

This directory (`ccsps/ccs-partners-`) is DECOMMISSIONED and must NOT be used to serve the CCS Connect application.

The single source of truth for the CCS Connect application is located at the workspace root:
`C:\Users\bansa\Desktop\CCS`

Canonical Application Endpoint:
`http://localhost:3000/`

Backend Endpoint:
`http://localhost:8000/api`
